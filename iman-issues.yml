name: Impulsar TikTok

on:
  issues:
    types: [opened, edited]

jobs:
  impulso:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v3

      - name: Ejecutar módulo de impulso
        run: echo "Iniciando impulso TikTok con XOXO (sin exponer secretos)"

      - name: Notificar en Discord (simulado)
        env:
          DISCORD_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          echo "Simulando envío a Discord desde XOXO (sin exponer datos reales)"
